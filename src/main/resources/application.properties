spring.application.name=Back

# Connexion à PostgreSQL
spring.datasource.url=jdbc:postgresql://aws-0-eu-west-3.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.uxeeyunyauhrdnkckrej
spring.datasource.password=ak?C9N$TCBs?

# JPA
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# Active le logging Flyway
logging.level.org.flywaydb.core=DEBUG
logging.level.org.springframework.boot.autoconfigure.flyway=DEBUG

# Vide le pool de connection JDBC
spring.datasource.hikari.auto-commit=true
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.pool-name=HikariCP
spring.datasource.hikari.connection-test-query=SELECT 1

# Cela désactive le redémarrage à chaud automatique.
# Ensuite, un clic sur le bouton Run (ou Ctrl+F5) dans IntelliJ fera un vrai redémarrage propre.
spring.devtools.restart.enabled=false

# Server (optionnel)
#server.port=8080
